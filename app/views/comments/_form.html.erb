<%= form_with(model: [ @post, @post.comments.build ], local: true) do |form| %>
    <div class="form-group">
      <%= form.text_area :body, class: 'form-control', placeholder: 'Your comment here...' %>
    </div>
    <div class="form-group">
      <%= form.submit 'Post Comment', class: 'btn btn-primary' %>
    </div>
<% end %>
  